spring.application.name=billing service

server:
  port: 0
spring:
  rabbitmq:
    host: localhost
    port: 5672
    username: dev
    password: dev
management:
  endpoints.web.exposure.include: health,info,metrics
