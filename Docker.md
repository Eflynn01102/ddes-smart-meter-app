# Docker Notes

To deploy the compose script run the following command

```bash
docker compose -f deployment/docker-compose.yml up -d
```

To compile the docker files for each service, enter each service and read the documentation to follow the instruction.
